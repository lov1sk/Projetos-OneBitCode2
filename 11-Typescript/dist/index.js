/*
const spaceship1 = {
  name: "Spaceship 1",
  pilot: "",
  speed: 0,
  inMission: false,
};
const spaceship2 = {
  name: "Spaceship 2",
  pilot: "",
  speed: 0,
  inMission: false,
};
function setPilot(
  pilot: String,
  spaceship: { name?: String; pilot?: String; speed?: number; inMission?: any }
) {
  spaceship.pilot = pilot;
}
function accelerate(
  targetSpeed: number,
  spaceship: { name?: String; pilot?: String; speed?: number; inMission?: any }
) {
  spaceship.speed = targetSpeed;
}
function sendToMission(
  inMission: boolean,
  spaceship: { name?: String; pilot?: String; speed?: number; inMission?: any }
) {
  spaceship.inMission = true;
}

setPilot("Lucas", spaceship1);
setPilot("Cardoso", spaceship2);
accelerate(150, spaceship1);
accelerate(100, spaceship2);
console.log(spaceship1);
console.log(spaceship2);
*/
